import LoginView from '@/components/LoginView';

// Swedish login — reached from the Swedish landing, the footer toggle,
// or first-visit language detection on /login.

export default function LoginPageSv() {
  return <LoginView locale="sv" />;
}
