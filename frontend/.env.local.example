# JobFinderOS frontend environment
# Copy to .env.local

# Backend API URL (default: local FastAPI server).
# NOTE: baked at BUILD time into the static export — on Cloudflare Pages
# it is set in the project's build environment variables (WO-07 runbook),
# NOT in a committed file.
NEXT_PUBLIC_API_URL=http://localhost:8000
